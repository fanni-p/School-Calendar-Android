<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.finalproject.schoolcalendar"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity" />
        <activity
            android:name=".activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.WeekScheduleActivity"
            android:noHistory="true" />
        <activity
            android:name=".activities.AllSubjectsActivity"
            android:noHistory="true" />
        <activity
            android:name=".activities.AllHomeworkActivity"
            android:noHistory="true" />
        <activity android:name=".activities.AddSubjectActivity" />
        <activity android:name=".activities.EditSubjectActivity" />
        <activity android:name=".activities.AddHomeworkActivity" />
        <activity android:name=".activities.EditHomeworkActivity" />
    </application>
</manifest>
